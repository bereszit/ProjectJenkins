name: My Workflow
on:
  pull_request:
    types:
      - closed

jobs:
  if_merged:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    steps:
      - id: trigger-jenkins
        run: |
          CRUMB=`curl -s -u "admin:${{ secrets.JENKINS_TOKEN }}" https://jenkins-dev.flowworks.com:443'/crumbIssuer/api/xml?xpath=concat(//crumbRequestField,":",//crumb)'`
          echo $CRUMB
          EX=$(curl -I -s -X POST -u "admin:${{ secrets.JENKINS_TOKEN }}"  -H "$CRUMB" "https://jenkins-dev.flowworks.com:443"/job/"TestBuildKit"/build)
          echo $EX

